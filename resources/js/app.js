// Load Laravel Breeze bootstrap helpers (axios, echo, etc.)
import "./bootstrap";

// Load Bootstrap JS (dropdowns, modals, tooltips)
import "bootstrap";

// Load custom CSS overrides
import "../css/app.css";

// Optional: Alpine.js for reactive components
import Alpine from "alpinejs";

window.Alpine = Alpine;
Alpine.start();
